#!/bin/sh
# Aave 监控入口。确保 Node 的内置 fetch 遵守 HTTPS_PROXY 环境变量
# （Node 默认忽略它们，表现为连不上 api.telegram.org 的 fetch failed）
# 用法：./aave/monitor check | watch | chat-id ...
exec env NODE_USE_ENV_PROXY=1 node "$(cd "$(dirname "$0")" && pwd)/index.js" "$@"
