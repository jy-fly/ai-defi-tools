# ── Aave 的 Telegram 推送 ──
# bot 叫 aave_risk_bot，专供 Aave 风控报警，所以用 AAVE_ 前缀
# 1) 找 @BotFather -> /newbot 拿 token
# 2) 把 bot 拉进群 / 或私聊发一条消息（必做，否则拿不到 chat_id）
# 3) 跑 `./aave/monitor chat-id` 列出 chat_id（群是负数）
AAVE_TELEGRAM_BOT_TOKEN=
AAVE_TELEGRAM_CHAT_ID=

# 可选兜底：多个协议想共用同一个 bot / 同一个群时，设这两个不带前缀的。
# 优先级：AAVE_TELEGRAM_* > TELEGRAM_*
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── 链 RPC（按链命名，同一条链上的多个协议共用）──
# 多个用逗号分隔，按顺序故障转移
ETH_RPC_URLS=https://ethereum-rpc.publicnode.com,https://eth.drpc.org

# ── MongoDB（可选，不设则完全不写 mongo）──
# 含密码，所以只放这里 / GitHub Secrets，绝不写进 config.json
# Atlas 免费层：Network Access 要加 0.0.0.0/0（Actions runner 的 IP 是动态的），
# 并且只给这个用户授 readWrite 单库权限，别用 admin
# MONGODB_URI=mongodb+srv://user:pass@cluster.xxxxx.mongodb.net/

# MongoDB 驱动走 TCP，不认 HTTPS_PROXY，只支持 SOCKS5。
# 国内本地测试连 Atlas 基本需要它；部署在海外服务器/Actions 上不用设。
# MONGODB_PROXY=socks5://127.0.0.1:7890

# ── 代理（国内网络必需）──
# api.telegram.org 需要代理，且 Node 的内置 fetch 默认忽略代理环境变量。
# 推荐写进 ~/.zshrc 做成全局；或用 ./aave/monitor 启动（自带 NODE_USE_ENV_PROXY=1）
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
# NODE_USE_ENV_PROXY=1
